sp {avoid*infinite*no*change*loop
   (state <s> ^impasse no-change
              ^superstate <ss>)
  -(<ss> ^name selection)
-->
   (write (crlf) (cmd print --depth 2 I2))
  #(halt)
  (write (crlf) | Avoid infinite loop of no-change state | (cmd print --depth 2 <s>) ||)
  (interrupt)
}