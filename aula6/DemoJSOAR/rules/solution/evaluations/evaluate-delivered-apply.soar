sp {solution*evaluate*state*success
   (state <s> # ^tried-tied-operator
              ^desired <d>
              ^delivered true
              ^traveledDistance <traveledDistance>)
-->
   (<s> ^operator <o> +)
   (<o> ^name applyCommand)
#   (write (crlf) | ********** Success! ********** |)
   (write (crlf) | ****************************** |)
   (write (crlf) | Total traveledDistance =  | <traveledDistance> ||)
   (write (crlf) | ****************************** |)
#   (halt)
}
