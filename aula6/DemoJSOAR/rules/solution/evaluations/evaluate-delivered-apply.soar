sp {solution*evaluate*state*success
   (state <s> # ^tried-tied-operator
              ^desired <d>
              ^delivered true
              ^traveled-distance <traveled-distance>)
-->
   (<s> ^operator <o> +)
   (<o> ^name output-command)
   (write (crlf) | ****************************** |)
   (write (crlf) | Total traveled-distance =  | <traveled-distance> ||)
   (write (crlf) | ****************************** |)
}
