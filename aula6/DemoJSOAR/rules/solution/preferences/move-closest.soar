# Always move to the closest target
sp {preference*move-closest
   (state <s> ^attribute operator
              ^impasse tie
              ^item <o> {<> <o> <o2>}
              ^superstate <ss>)
   (<o> ^name move)
   (<o2> ^name move)
   (<o2> ^parameter <parameter2>)
   (<parameter2> ^DISTANCE <distance2>)
   (<o> ^parameter <parameter>)
   (<parameter> ^DISTANCE { <distance> < <distance2> })
-->
   (<ss> ^operator <o> > <o2>)}

